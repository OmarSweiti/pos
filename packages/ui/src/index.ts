// Shared design-system components land here (blueprint §2).

// The internationalisation contract both front ends resolve against
// (microstep 1.11.1, gap G-5).
export {
  type Catalog,
  DEFAULT_LOCALE,
  type Direction,
  directionFor,
  LOCALES,
  type Locale,
} from "./i18n";
